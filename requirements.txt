# Python Dependencies for Cruzber BQML Pipeline
# Track A: OOS Alerting System (h=4 weeks)

# Core BigQuery client (ADC authentication)
google-cloud-bigquery>=3.11.0

# Optional: Enhanced table operations
db-dtypes>=1.1.0

# CLI framework (used by src/entrypoint.py)
click>=8.1.0

# Environment variable loading
python-dotenv>=1.0.0

# Pandas + PyArrow: required for cross-region KPI snapshot bridge
pandas>=2.0.0
pyarrow>=12.0.0

